# Optional. Defaults to `codex` from PATH.
CODEX_BIN=/Applications/Codex.app/Contents/Resources/codex

# Optional. Comma-separated brand choices shown in the product master form.
NEXT_PUBLIC_BRAND_OPTIONS=Brand A,Brand B
